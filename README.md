A weather API client built with Python, HTTPX, and OOP.
The project uses Open-Meteo API to retrieve current weather and multi-day forecasts,
with separate classes for HTTP requests, weather logic, and data models
